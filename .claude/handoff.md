## Последняя сессия: 2026-03-13

### Что сделали
Полный рефакторинг с нуля — удалён стандартный режим, переписан пайплайн:

- **`modules/llm_analyzer.py`** — переписан с нуля: чанки по 30 сегментов, шаг 25, перекрытие 5. LLM получает Whisper-сегменты (sentence-level), возвращает `{"delete": [...]}`. Никакого text-matching.
- **`modules/transcriber.py`** — добавлен `_compute_boundaries()`: точные start/end из word timestamps (`first_word.start - 0.05`, `last_word.end + 0.15`, cap `_MAX_WORD_DUR=1.5`). Clipping: сегмент N не может перекрывать N+1.
- **`main.py`** — переписан: `segments → analyzer.analyze(segments) → kept → renderer`. Без build_blocks, без стандартного режима.
- **`modules/bot.py`** — удалён стандартный режим полностью: `_mode`, `_MODE_KEYBOARD`, `_on_mode_selected`, конструктор изменён на `pipeline_fn` (один аргумент вместо двух).
- **`modules/session_manager.py`** — удалены `StandardSession`, `scan_standard_sessions()`, `concat_standard_files()`.
- **`config.py`** — удалены `STANDARD_*` и `BLOCK_*` константы.
- **Удалены файлы**: `modules/timeline.py`, `modules/standard_pipeline.py`.

### Статус деплоя
✅ Задеплоено на сервер 85.239.33.163, сервис `automontazh` запущен и работает.
Commit: `99356aa` — "refactor: переписать пайплайн с нуля, удалить стандартный режим"

---

## На завтра / следующая сессия

### Приоритет 1 — Разобраться почему LLM оставляет слишком много
Тест показал: 149/165 сегментов оставлено (28:12 из 30:34). Ожидалось 13-15 минут.
Старая система срезала паузы на уровне слов (build_blocks + порог 0.4с) — отсюда 50% сокращение.
Новая система режет только целые сегменты (sentence-level), пауз между словами не трогает.

**Варианты решения (обсудить с пользователем):**
- A) Сделать промпт агрессивнее: удалять не только повторы, но и мусор/хезитации/навигацию
- B) Добавить post-processing: резать межсловные паузы внутри оставленных сегментов через FFmpeg silencedetect
- C) Гибрид: word-level блоки + dedup на границах сегментов Whisper

### Открытые вопросы
- Промпт работает правильно (пользователь отклонил агрессивную правку). Почему тогда 28 мин — из-за архитектуры (sentence-level vs word-level) или промпт недостаточно агрессивен?
- Технические повторы ("графикой-графикой") — исправлены ли они в новой версии? Пользователь сказал "даже смотреть не буду" — результат не проверялся детально.

### Следующий этап
После решения проблемы с длиной — тестирование нескольких видео, затем обновление CLAUDE.md.

---

## Обещания / договорённости
- Все изменения только через git (локально → push → deploy), никаких прямых правок на сервере
- Стандартный режим удалён навсегда
- Промпт LLM трогать только после обсуждения — пользователь отклонил агрессивную правку
